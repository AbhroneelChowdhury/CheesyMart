import { PricePerKiloDirective } from './price-per-kilo.directive';

describe('PricePerKiloDirective', () => {
  it('should create an instance', () => {
    const directive = new PricePerKiloDirective();
    expect(directive).toBeTruthy();
  });
});
