import { CamelSpacePipe } from './camel-space.pipe';

describe('CamelSpacePipe', () => {
  it('create an instance', () => {
    const pipe = new CamelSpacePipe();
    expect(pipe).toBeTruthy();
  });
});
